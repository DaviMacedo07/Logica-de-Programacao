let array = []
let arrayInvertido = []
let quantidade = parseInt(prompt("Digite a quantidade de números a serem inseridos no array"))

for (let i = 0 ; i < quantidade ; i++) {
    let numero = parseInt(prompt(""))
}